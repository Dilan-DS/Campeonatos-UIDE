# Dependencias solo para desarrollo y CI (no se instalan en producción).
-r requirements.txt

coverage==7.6.10

# E2E (Playwright sobre pytest, reutilizando el mismo entorno Python/Django
# del proyecto en vez de anadir un toolchain de Node aparte).
pytest==8.3.5
pytest-django==4.9.0
pytest-playwright==0.5.2
playwright==1.49.1
